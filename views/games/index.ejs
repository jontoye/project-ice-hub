<h1>League Schedule</h1>

<% if (games.length == 0) { %>
    <p class="lead">There are no games yet.</p>
<% } else { %>

    <div class="row">
        <div class="col-9">
            <table class="table">
                <thead>
                    <tr>
                        <th>Score</th>
                        <th>Home</th>

                        <th>Date</th>

                        <th>Away</th>
                        <th>Score</th>

                        <th></th>
                        <th></th>

                    </tr>
                </thead>

                <tbody>
                    <% games.forEach(game => { %>
                        <tr>
                            <td><%= game.home_team_score %> </td>
                            <td><%= game.home_team.fullname %> </td>
                            
                            <td><%= game.gametime %> </td>

                            <td><%= game.away_team.fullname %> </td>
                            <td><%= game.away_team_score %> </td>

                            <% if (game.is_complete) { %>
                                
                                <td>FINAL</td>
                             
                            <% } else {%>

                                <td>
                                    <a href="/games/<%= game._id %>/update">
                                        <img src="/images/icon-update.png" alt="icon-update">
                                    </a>
                                </td>
    
                                <td>
                                    <a href="/games/<%= game._id %>/delete">
                                        <img src="/images/icon-delete.png" alt="icon-delete">
                                    </a>
                                </td>
                            <% } %>
                            
                        </tr>
                    <% }) %>
                </tbody>
            </table>
        </div>
    </div>

<% } %>